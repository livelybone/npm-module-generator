# {{projectName}}
[![NPM Version](http://img.shields.io/npm/v/{{projectName}}.svg?style=flat-square)](https://www.npmjs.com/package/{{projectName}})
[![Download Month](http://img.shields.io/npm/dm/{{projectName}}.svg?style=flat-square)](https://www.npmjs.com/package/{{projectName}})
![gzip with dependencies: kb](https://img.shields.io/badge/gzip--with--dependencies-kb-brightgreen.svg "gzip with dependencies: kb")
![typescript](https://img.shields.io/badge/typescript-supported-blue.svg "typescript")
![pkg.module](https://img.shields.io/badge/pkg.module-supported-blue.svg "pkg.module")

> `pkg.module supported`, which means that you can apply tree-shaking in you project

[中文文档](./README-CN.md)

{{description}}

## repository
{{repository}}

## Demo
{{homepage}}

## Run Example
you can see the usage by run the example of the module, here is the step:

1. Clone the library `git clone {{repository}}`
2. Go to the directory `cd your-module-directory`
3. Install npm dependencies `npm i`(use taobao registry: `npm i --registry=http://registry.npm.taobao.org`)
4. Open service `npm run dev`
5. See the example(usually is `http://127.0.0.1:3000/examples/test.html`) in your browser

## Installation
```bash
npm i -S {{projectName}}
```

## Global name - The variable the module exported in `umd` bundle
`{{GlobalName}}`

## Interface
See what method or params you can use in [index.d.ts](./index.d.ts)

## Usage
```js
import * as {{GlobalName}} from '{{projectName}}'
```

Use in html, see what you can use in [CDN: unpkg](https://unpkg.com/{{projectName}}/lib/umd/)
```html
<-- use what you want -->
<script src="https://unpkg.com/{{projectName}}/lib/umd/<--module-->.js"></script>
```

Or，see what you can use in [CDN: jsdelivr](https://cdn.jsdelivr.net/npm/{{projectName}}/lib/umd/)
```html
<script src="https://cdn.jsdelivr.net/npm/{{projectName}}/lib/umd/<--module-->.js"></script>
```
