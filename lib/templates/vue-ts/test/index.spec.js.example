import { shallowMount } from '@vue/test-utils'
import {{ModuleName}} from '../src/components/Index.vue'

describe('Index.vue', () => {
  it('Rendered', () => {
    const wrapper = shallowMount({{ModuleName}})
    expect(wrapper.find('div').exists()).to.equal(true)
  })
})
